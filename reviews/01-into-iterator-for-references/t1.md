Spec: 01-into-iterator-for-references
Task: t1
Commit: f0370db2bdace3349868968709ac9e54025cb474
Verdict: pass

none

## Spec conformance

- Scenario → test mapping (all in `tests/acceptance.rs`, named after the scenario):
  - Iterating a borrowed Some yields a reference to its value → `iterating_a_borrowed_some_yields_a_reference_to_its_value`
  - Iterating a borrowed Null yields nothing → `iterating_a_borrowed_null_yields_nothing`
  - Iterating a borrowed Absent yields nothing → `iterating_a_borrowed_absent_yields_nothing`
  - A borrowed Presence is accepted where IntoIterator is required → `a_borrowed_presence_is_accepted_where_into_iterator_is_required`
- Scope creep: none. The `tests/iterator_tests.rs` change (`some.iter()` → `&some`) is required by pedantic `clippy::explicit_iter_loop` once the impl exists; no behavior change.
- Invariants: T1 owns none; the equivalence property belongs to T3.
- Public API matches the spec: `Item = &'a T`, `IntoIter = Iter<'a, T>`; the `#[expect(clippy::iter_without_into_iter)]` on `Presence::iter` is removed.

## Generic review

`code-review` (medium) on `src/presence.rs`, `tests/acceptance.rs`, `tests/iterator_tests.rs`: no findings.
OCR delegate review of the same files: no findings.
