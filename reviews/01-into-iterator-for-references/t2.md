Spec: 01-into-iterator-for-references
Task: t2
Commit: d65228bd391b4f312a965f9bcdbcc098cfa64319
Verdict: pass

none

## Spec conformance

- Scenario → test mapping (all in `tests/acceptance.rs`, named after the scenario):
  - Iterating a mutably borrowed Some allows modifying its value → `iterating_a_mutably_borrowed_some_allows_modifying_its_value`
  - Iterating a mutably borrowed Null or Absent yields nothing and leaves the state unchanged → `iterating_a_mutably_borrowed_null_or_absent_yields_nothing_and_leaves_the_state_unchanged`
- Scope creep: none. The `tests/iterator_tests.rs` changes (`some.iter_mut()` → `&mut some`, `null.iter_mut()` → `&mut null`) are required by pedantic `clippy::explicit_iter_loop` once the impl exists; no behavior change.
- Invariants: T2 owns none; the equivalence property belongs to T3.
- Public API matches the spec: `Item = &'a mut T`, `IntoIter = IterMut<'a, T>`; the `#[expect(clippy::iter_without_into_iter)]` on `Presence::iter_mut` is removed.

## Generic review

`code-review` (medium) on `src/presence.rs`, `tests/acceptance.rs`, `tests/iterator_tests.rs`: no findings.
OCR delegate review of the same files: no findings.

## Mutation testing

`keeler mutants-diff 52aebae`: 4 mutants, 0 caught, 0 missed, 0 timeout, 4 unviable. Every mutant replaces the one-line body of `into_iter` with an `IterMut` constructor that does not exist (`new`, `from`, `from_iter`, `Default`), so none compiles; the delegation to `iter_mut()` is pinned by the acceptance tests and the doctest.
