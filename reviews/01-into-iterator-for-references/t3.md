Spec: 01-into-iterator-for-references
Task: t3
Commit: dd69f880a10275a0dacae5580998fdcceca28b53
Verdict: pass

none

## Spec conformance

- Scenario → test mapping:
  - Reference iteration matches iter() and iter_mut() → `presence::tests::reference_iteration_matches_iter_and_iter_mut` (proptest, `src/presence.rs`)
  - The clippy suppression is no longer needed → `the_clippy_suppression_is_no_longer_needed` (`tests/acceptance.rs`), backed by the lint gate (`keeler lint`, pedantic, `-D warnings`)
- Invariants from Implementation Notes, all pinned by the property over `Some(any::<i32>())` / `Null` / `Absent`: items equal, `len()` equal, `size_hint()` equal, item count is 1 iff `is_present()` — for `&p` vs `iter()` and `&mut p` vs `iter_mut()`.
- `Iter` / `IterMut` struct docs now mention creation through `IntoIterator` for references; `CHANGELOG.md` has `## [Unreleased]` → `### Added`.
- Scope creep: none. T3 adds no production code.

## Generic review

`code-review` (medium) on `src/presence.rs`, `tests/acceptance.rs`, `CHANGELOG.md`: no findings.
Observation, accepted as is: `the_clippy_suppression_is_no_longer_needed` counts the text `iter_without_into_iter` in the source, so a harmless mention of the lint name would fail it. It can only fail falsely, never pass falsely, so it errs on the safe side.
OCR delegate review of the same files: no findings.

## Mutation testing

`keeler mutants-diff d867561` (T3 diff): no mutants — the diff holds only docs and a `#[cfg(test)]` module.
`keeler mutants-diff chore/keeler-setup` (whole feature, T1–T3): 8 mutants, 0 caught, 0 missed, 0 timeout, 8 unviable — every mutant replaces a one-line `into_iter` body with an `Iter` / `IterMut` constructor that does not exist.
